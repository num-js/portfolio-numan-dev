"use client";

import CirclesBackdrop from "@/components/shared/CirclesBackdrop";

/** Footer-facing re-export of the shared orb backdrop (right-anchored). */
export default function FooterCirclesBackdrop() {
  return <CirclesBackdrop side="right" />;
}
